<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jgajardo.creditobienraiz">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CasaActivity" />
        <activity android:name=".DepartamentoActivity" />
        <activity android:name=".InfoCasaActivity" />
        <activity android:name=".FormActivity" />
        <activity android:name=".ResumeActivity" />
        <activity android:name=".FormDeptoActivity" />
        <activity android:name=".InfoDptoActivity" />
        <activity android:name=".ResumenDptoActivity"></activity>
    </application>

</manifest>